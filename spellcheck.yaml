matrix:
- name: Spellcheck
  aspell:
    lang: it
    ignore-case: true
  # dictionary:
  #   wordlists:
  #   - wordlist.txt
  #   encoding: utf-8
  sources:
    - '**/*.tex'
  default_encoding: utf-8
